# MIMXRT1170-EVKB (Cortex-M7) board config for the telemetry demo -- ETHERNET.
#
# Unlike the MCXN947, the RT1170-EVK's 100M ENET MAC + onboard PHY are already
# status="okay" in the board DTS, so NO overlay is needed to bring up the link.
# This .conf just selects the NXP ENET driver + L2 and turns on DHCPv4 so the
# interface self-configures and the SDK's connection manager sees L4 up.
#
# Board target: mimxrt1170_evk/mimxrt1176/cm7 (revision B default).

# --- Bearer: native Ethernet (RT1170 ENET, RMII, onboard PHY) --------------
CONFIG_NET_L2_ETHERNET=y
CONFIG_ETH_NXP_ENET=y
CONFIG_MDIO=y

# --- IPv4 via DHCP; auto-bring-up the interface ----------------------------
CONFIG_NET_DHCPV4=y
CONFIG_NET_CONFIG_AUTO_INIT=y
CONFIG_NET_CONFIG_NEED_IPV4=y
CONFIG_NET_CONFIG_SETTINGS=y

# --- Net buffers sized for a wired link ------------------------------------
CONFIG_NET_PKT_RX_COUNT=16
CONFIG_NET_PKT_TX_COUNT=16
CONFIG_NET_BUF_RX_COUNT=32
CONFIG_NET_BUF_TX_COUNT=32
CONFIG_NET_CONTEXT_RCVTIMEO=y

# --- mbedTLS heap headroom for cloud broker cert chains --------------------
CONFIG_MBEDTLS_HEAP_SIZE=60000
